<%= @user.name %>
<%= attachment_image_tag @user, :image, :fill, 60, 60, format: 'jpeg' %>
<% if @user.id == current_user.id %>
  <%= link_to "編集", edit_user_path %>
<% end %>
<%= link_to "退会する", unsubscribe_path %>