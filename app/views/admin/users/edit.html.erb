<h2>プロフィール編集</h2>
<%= form_with model:@user, local:true do |f| %>
  <h4>名前</h4>
  <%= f.text_field :name %>
  <h4>退会処理</h4>
  <%= f.select :is_valid, [['退会', false], ['有効', true]] %>
  <%= f.submit "変更を保存" %>
<% end %>